#include "main.h"

/**
 *  _strlen -  returns the length of a string.
 *  @s: the string
 *  @strlen: the command to get lenght
 *  Return: Length of strlen
 */
int _strlen(char *s)
{
	size_t length = 0;
	while (*str++)
	length++;
	return (length);
}
